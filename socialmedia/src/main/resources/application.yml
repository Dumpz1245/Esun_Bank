spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/esun_bank?useSSL=false&serverTimezone=UTC
    username: vscode_user
    password: passwordvs
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8081
